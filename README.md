# FastAPI-Patient-Management
A modern Patient Management System built with FastAPI and Vanilla JavaScript. Features a real-time dashboard to track health metrics like BMI, manage patient records (CRUD operations), and search patient data using a JSON-based local database.
